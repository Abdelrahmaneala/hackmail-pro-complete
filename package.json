{
  "name": "hackmail-pro-complete",
  "version": "5.0.0",
  "description": "Complete Temporary Email System with MongoDB & Real Services",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "mkdir -p public && cp index.html public/ && cp script.js public/ && cp style.css public/ && echo 'Build completed'",
    "vercel-build": "mkdir -p public && cp index.html public/ && cp script.js public/ && cp style.css public/ && echo 'Vercel build completed'"
  },
  "dependencies": {
    "axios": "^1.5.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.3",
    "helmet": "^7.0.0",
    "jsdom": "^27.0.1",
    "mongoose": "^7.5.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "keywords": [
    "email",
    "temporary",
    "mongodb",
    "hackmail",
    "real-service",
    "disposable-email"
  ],
  "author": "HackMail Team",
  "license": "MIT"
}